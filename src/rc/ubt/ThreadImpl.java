package rc.ubt;

public class ThreadImpl extends Thread
{
	//this class will implement thread that does not have fixed entry point and execute method by string name
	//also this thread will be able to store any class for reference
	//class instance and string name for method to be exactly
}
